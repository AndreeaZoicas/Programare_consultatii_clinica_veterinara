@page
@model Programare_consultatii_clinica_veterinara.Pages.Medici.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Medic[0].Nume)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Medic[0].Specializare)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Medic[0].ExperientaAni)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Medic) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nume)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Specializare)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExperientaAni)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.MedicID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.MedicID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.MedicID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
